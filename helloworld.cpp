// Includes the IO library
#include <iostream>

// The main function
int main()
{
    std::cout << "Hello, world !" << std::endl; // => Hello, world !

    // Returns 0, the successful code (return any integer between 1 and 255 for an error)
    return 0;
}